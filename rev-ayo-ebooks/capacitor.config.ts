import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.cvi.bookstore',
  appName: 'rev-ayo-ebooks',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
